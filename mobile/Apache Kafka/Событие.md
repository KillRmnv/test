- Основная единица данных в Kafka.
    
- Состоит из: **Ключа** (необязательно, используется для определения раздела), **Значения** (тело сообщения, например, JSON, Avro), **Заголовков** (метаданные) и **Метки времени**.