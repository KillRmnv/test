1. FROM в [[Dockerfile]] автоматически создает новый образ, т.е. в [[Dockerfile]] если реализована [[Многоступенчатая сборка (Multi-stage builds)]] будет создано несколько образов(по одному на стадию)
2. **Docker по умолчанию дает имя (тег) только последнему этапу (образу), потому что именно его ты собираешься использовать.** Остальные — технические артефакты, как строительные леса — они важны в процессе, но не нужны в готовом здании.Теги для этапов можно указывать в команде запуска `docker build -t` .Также можно после сборки дать тег